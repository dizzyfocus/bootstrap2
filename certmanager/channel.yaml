apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: cert-manager-channel
  namespace: openshift-cert-manager-operator
spec:
  type: Git
  pathname: "https://github.com/openshift/cert-manager-operator"
